cmake_minimum_required(VERSION 3.12)
project(Affine)
file(GLOB "src_image.jpg")
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIR})
add_executable(main main.cpp)
target_link_libraries(main ${OpenCV_LIBS})
